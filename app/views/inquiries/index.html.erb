<h6>for admin User please check the (website)</h6>

<table>
  <tr>
    <th>Company name</th>
    <th>Company address</th>
    <th>Contact person</th>
    <th>Contact number</th>
    <th>Inquiry</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @inquiries.each do |inquiry| %>
  <tr>
    <td><%= inquiry.company_name %></td>
    <td><%= inquiry.company_address %></td>
    <td><%= inquiry.contact_person %></td>
    <td><%= inquiry.contact_number %></td>
    <td><%= inquiry.inquiry %></td>
    <td><%= link_to 'Show', inquiry %></td>
    <td><%= link_to 'Edit', edit_inquiry_path(inquiry) %></td>
    <td><%= link_to 'Destroy', inquiry, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Create New Inquiry', new_inquiry_path %>
