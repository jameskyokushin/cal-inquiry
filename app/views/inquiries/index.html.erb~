<div id="form1">
<br />
<br />
<div id="link1">
<%= link_to 'listing Inquiries', inquiries_path %>||
<%= link_to 'Create New Service', new_inquiry_path %>
</div>
<div id="link">
<%= link_to "Sign out",  destroy_user_session_path,  :method=> :delete %> ||
<%= current_user.email if user_signed_in? %>
</div>
<br />
<br />
<table>
  <tr>
    <th>Company</th>
   
    <th>Contact person</th>
   
    <th>Prepared by</th>
    <th>Status</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @inquiries.each do |service| %>
  <tr>
    <td><%= inquiry.company %></td>
   
    <td><%= inquiry.contact_person %></td>
   
    <td><%= inquiry.prepared_by %></td>
    <td><%= inquiry.status %></td>
    <td><%= link_to 'Preview', service %></td>
    <!--<td><%= link_to 'Edit', edit_inquiry_path(inquiry) %></td>
    <td><%= link_to 'Destroy', service, :confirm => 'Are you sure?', :method => :delete %></td>-->
  </tr>
<% end %>
</table>

<br />
</div>

